# ============================================================
# Meego Lite 环境变量示例
# 使用方法：复制本文件为 .env，按需修改值
#   cp .env.example .env
# ============================================================

# ===== 数据库 =====
# 默认本地 SQLite，无需修改
# 如需 PostgreSQL（云端部署），改成：
#   postgresql://user:password@host:5432/dbname?sslmode=require
DATABASE_URL="file:./dev.db"

# ===== NextAuth 鉴权（如启用登录） =====
# 用 `openssl rand -base64 32` 生成一串随机字符串
NEXTAUTH_SECRET="please-change-me-to-a-random-string"
NEXTAUTH_URL="http://localhost:3000"

# ===== SMTP 邮件通知（可选，启用邮件渠道时必填） =====
# 以 QQ 邮箱为例：登录 QQ 邮箱 → 设置 → 账户 → 开启 IMAP/SMTP 服务 → 获取 16 位授权码
# 注意：
#   1) SMTP_FROM 中的邮箱必须等于 SMTP_USER，否则 QQ 邮箱会拒收
#   2) 不要使用 ${SMTP_USER} 这种变量插值写法，请直接写完整地址
#   3) 若 465 端口被封禁，可改用 SMTP_PORT=587 + SMTP_SECURE=false（STARTTLS）
SMTP_HOST=smtp.qq.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your_account@qq.com
SMTP_PASS=your_16_digit_auth_code
SMTP_FROM="Meego Lite <your_account@qq.com>"

# ===== 企业微信应用消息通知（可选，启用企业微信个人通知时必填） =====
# 企业微信管理后台 → 应用管理 → 自建应用，获取企业 ID、AgentId、Secret
# 用户侧需在「设置 → 通知偏好」填写自己的企业微信用户 ID（userid）
WECOM_CORP_ID=your_corp_id
WECOM_AGENT_ID=1000002
WECOM_APP_SECRET=your_app_secret

# ===== JWT 会话签名密钥（登录鉴权必填） =====
# 用 `openssl rand -base64 32` 生成一串随机字符串，用于 JWT 会话签名
AUTH_SECRET="please-change-me-to-a-random-32+char-string"

# ===== 会话 Cookie 安全标志（按部署协议设置） =====
# - HTTP 部署（局域网/内网/本地）：保持 false 或不设置，否则浏览器会丢弃 Cookie 导致登录后无法跳转
# - HTTPS 部署（生产环境）：设为 true，仅在 HTTPS 下传输 Cookie，更安全
COOKIE_SECURE=false


# 临时：开启报告 Mock 模式，点击「生成报告」直接返回示例 Markdown，不调用真实 OpenAI（用于前端效果预览）
# 配置好真实 OPENAI_API_KEY 后将其设为 0 或删除本行即可走真实 AI 生成
# REPORTS_MOCK=1
