# Checklist

- [x] 新增 `KanbanGroupBy` 类型与 `KANBAN_GROUP_BY_LABEL` 标签映射，覆盖状态/项目/负责人/优先级/标签五个维度
- [x] 看板模式下展示「分组方式」切换控件，列表模式下不显示该控件
- [x] 默认进入看板时按状态分组，列顺序为 待开始/进行中/已暂停/已完成/已关闭，与原有行为一致
- [x] 切换「项目」分组：按项目生成列，无项目任务归入「暂不关联」列，列头显示项目名与数量
- [x] 切换「负责人」分组：按责任人生成列，未分配任务归入「未分配」列
- [x] 切换「优先级」分组：生成 P0/P1/P2/P3 列并正确归类
- [x] 切换「标签」分组：按标签生成列，多标签任务在各自标签列重复出现，无标签任务归入「无标签」列
- [x] 优先级分组下拖拽卡片调用 PUT 更新 `priority` 并刷新数据、提示成功
- [x] 负责人分组下拖拽卡片更新 `assigneeId`（含拖到「未分配」置为 null）
- [x] 项目分组下拖拽卡片更新 `projectId`（含拖到「暂不关联」置为 null）
- [x] 标签分组下拖拽不触发任何字段更新（只读浏览，无 id 冲突报错）
- [x] 项目类型构建/类型检查通过，无新增报错
