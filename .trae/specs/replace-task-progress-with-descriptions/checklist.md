- [x] 「我的任务」列表表头已由“进度”改为“进度描述”，且单元格展示最新一条描述摘要
- [x] 「我的任务」列表中的进度描述列不展示状态标签，只展示描述文本
- [x] 无进度记录的任务在列表中显示“暂无进度描述”，不再显示任何百分比占位
- [x] `POST /api/progress` 与 `PATCH /api/progress/[id]` 仅接受有效描述内容，空描述会返回错误
- [x] `Task.progress` 与 `ProgressUpdate.percent` 已从 Prisma 模型、运行时 schema 兼容逻辑和相关接口类型中移除，`ProgressUpdate.status` 已补齐
- [x] 新增、编辑、删除进度记录后，不再触发任何百分比同步或任务主表回写逻辑
- [x] 创建任务成功后会自动生成首条过程记录，且记录状态等于任务初始状态
- [x] 每次任务状态更新后都会自动新增一条过程记录，且记录状态等于更新后的任务状态
- [x] 任务详情页不再展示进度条、`xx%` 标签或“完成进度”概览，只保留描述型时间线
- [x] 任务详情页和编辑页中的过程记录项会在原百分比位置展示对应的任务状态标签
- [x] 任务编辑弹窗中的进度区已移除百分比快捷档、数字输入和相关说明，仅保留文本记录能力
- [x] 看板任务卡片及其他任务概览视图不再展示百分比进度
- [x] AI 报告聚合和提示词文案中不再出现“进度 75%”“高进度项”等百分比措辞
- [x] 初始化数据、Mock/Seed 数据中不再写入 `progress` 或 `percent` 字段
- [x] `npm run lint` 通过
- [x] `npx tsc --noEmit` 通过
